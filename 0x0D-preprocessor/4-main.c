#include <stdio.h>
#include "4-sum.h"
#include "4-sum.h"

/**
 * main - Entry point
 *
 * Description: Checks the SUM macro by calculating the sum of two numbers
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int s;

	s = SUM(98, 1024);
	printf("%d\n", s);

	return (0);
}
