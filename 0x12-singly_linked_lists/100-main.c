#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
	printf("(A tortoise, having a pretty good sense of a hare's nature,");
	printf(" challenges one to a race.)\n");
	return (0);
}
